apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-hashing-apis
  namespace: kube-system
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cloud-hashing-apis
  template:
    metadata:
      labels:
        app: cloud-hashing-apis
    spec:
      containers:
      - name: cloud-hashing-apis
        image: uhub.service.ucloud.cn/entropypool/cloud-hashing-apis:latest
        imagePullPolicy: Always
        env:
        - name: ENV_ENVIRONMENT_TARGET
          valueFrom:
            configMapKeyRef:
              name: system-environment
              key: environment.target
        - name: ENV_CONSUL_HOST
          valueFrom:
            configMapKeyRef:
              name: system-environment
              key: environment.consul_host
        - name: ENV_CONSUL_PORT
          valueFrom:
            configMapKeyRef:
              name: system-environment
              key: environment.consul_port
        - name: ENV_CLUSTER_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace

---
apiVersion: v1
kind: Service
metadata:
  name: cloud-hashing-apis
  namespace: kube-system
spec:
  ports:
  - name: api
    port: 50140
    targetPort: 50140
  selector:
    app: cloud-hashing-apis
